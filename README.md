# meles

## Getting Started

**Meles** uses `uv` as its package manager.

```shell
# Install uv on your system (if not already installed)
curl -LsSf https://astral.sh/uv/install.sh | sh
```

```shell
# Install all dependencies
uv sync
```

```shell
# Format the code
uv run ruff format
```